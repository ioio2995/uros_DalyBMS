## IDF Component Manager Manifest File
dependencies:
  ## Required IDF version
  idf:
    version: ">=4.1.0"
  # # Put list of dependencies here
  # # For components maintained by Espressif:
  # component: "~1.0.0"
  # # For 3rd party components:
  # username/component: ">=1.0.0,<2.0.0"
  # username2/component2:
  #   version: "~1.0.0"
  #   # For transient dependencies `public` flag can be set.
  #   # `public` flag doesn't have an effect dependencies of the `main` component.
  #   # All dependencies of `main` are public by default.
  #   public: true
  ioio2995/ic_led_rgb:
    version: master
    git: https://github.com/ioio2995/ESP_IC_Led_RGB.git

  ioio2995/dalybms:
    version: master
    git: https://github.com/ioio2995/ESP_DalyBMS.git

  micro-ros/micro_ros_espidf_component:
    git: https://github.com/micro-ROS/micro_ros_espidf_component.git

component:
  description: "Daly SmartBMS Monitor - µROS"
  version: "1.0.0"